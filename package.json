{
	"name": "social-app-root",
	"version": "1.0.0",
	"description": "Root package for EngiConnect",
	"engines": {
		"node": ">=18.0.0"
	},
	"scripts": {
		"postinstall": "cd server && npm install",
		"start": "cd server && node server.js",
		"build": "echo 'No build step required for server'"
	}
}
